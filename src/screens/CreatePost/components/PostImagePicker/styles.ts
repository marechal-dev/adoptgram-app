import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  root: {
    marginTop: 24,
    marginBottom: 18,
  },
  placeholderText: {
    fontFamily: 'Poppins',
    fontSize: 16,
    textAlign: 'center',
  },
  actionContainer: {
    marginTop: 12,
    alignItems: 'center',
  },
});
